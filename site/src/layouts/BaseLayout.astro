---
import '../styles/global.css';
import { withBase } from '../lib/paths';

interface Props {
  title?: string;
  description?: string;
  image?: string;
}

const siteName = 'よけいラボ';
const description =
  Astro.props.description ?? '生活のすみっこで、余計なことを調べています。';
const title = Astro.props.title ? `${Astro.props.title} | ${siteName}` : siteName;
const image = Astro.props.image ?? '/images/site/editorial-desk.jpg';
const siteUrl = Astro.site ?? new URL(Astro.url.origin);
const imageUrl = new URL(withBase(image), siteUrl).toString();
const rssUrl = new URL(withBase('/rss.xml'), siteUrl).toString();
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content={description} />
    <meta property="og:site_name" content={siteName} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:image" content={imageUrl} />
    <meta property="og:url" content={Astro.url.href} />
    <meta name="twitter:card" content="summary_large_image" />
    <link rel="canonical" href={Astro.url.href} />
    <link rel="alternate" type="application/rss+xml" title={siteName} href={rssUrl} />
    <title>{title}</title>
  </head>
  <body>
    <div class="site-shell">
      <header class="site-header">
        <div class="site-header__inner">
          <a class="brand" href={withBase('/')} aria-label="よけいラボ home">
            <span class="brand__mark" aria-hidden="true"></span>
            <span>{siteName}</span>
          </a>
          <nav class="nav" aria-label="Primary">
            <a href={withBase('/')}>記事</a>
            <a href={withBase('/about/')}>編集部</a>
            <a href={withBase('/tags/')}>タグ</a>
            <a href={withBase('/rss.xml')}>RSS</a>
          </nav>
        </div>
      </header>
      <main class="main">
        <slot />
      </main>
      <footer class="site-footer">
        <div class="site-footer__inner">
          <span>よけいラボ</span>
          <span>よく見るものほど、いったん会議にかけます。</span>
        </div>
      </footer>
    </div>
  </body>
</html>
